import { requires } from './IRequirement';
import { TaskItem } from '@/models';

function addTaskItem(require: requires) {
    //
}

function readAllTaskItems(require: requires) {
    //
}

function updateTaskItem(require: requires) {
    //
}

function deleteTaskItem(require: requires) {
    //
}

export { addTaskItem, readAllTaskItems, updateTaskItem, deleteTaskItem };